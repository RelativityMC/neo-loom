pluginManagement {
	plugins {
		id 'org.relativitymc.neo-loom-repositories'
	}
}

plugins {
	id 'org.relativitymc.neo-loom-repositories'
}

dependencyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
}

include 'basic'
include 'projmap'